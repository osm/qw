//NFProxy player config file
//this is only called when changing from spectator to player mode!

say .echo 	off
say .clock 	2
say .centerpt 	off
say .enemyskin 	enemy
say .echo 	on

//executing my standart config here to restore my mouse bindings
//which have changed when NFProxy_spectator got called!

exec Niewi.txt
